# TK-017 evidence — auditoria de contração editorial

Status: `in_progress`; auditor e guard implementados, remoção deliberadamente
não executada antes do cutover.

## Entregue

- `audit_release_surface()` e `scripts/check_farol_v2_surface.py` detectam
  termos editoriais/legados sem despejar conteúdo de origem e aceitam allowlist
  apenas para migration/specs autorizados.
- `require_cutover_approved()` bloqueia contração sem decisão exata
  `cutover_approved` e `ok=true`.

## Verificação

- `tests/test_release_v2_surface.py` passou para findings RED, allowlist,
  ausência de source-line leakage e guard de cutover.
- No checkout pré-cutover, o auditor `all` encontrou 383 findings editoriais e
  1096 legados; isso é o bloqueio esperado, não um motivo para apagar arquivos.

## Pendente

Os itens 17.2–17.4 só podem ser executados após TK-018 emitir decisão
aprovada. Nenhum runtime, schema, vendor ou documentação foi removido.
