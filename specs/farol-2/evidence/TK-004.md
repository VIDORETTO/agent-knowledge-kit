# TK-004 evidence — IR canônica

Status: `in_progress`; a fatia local foi implementada e testada, mas a
dependência externa TK-001 continua `not_run`.

## Entregue

- `docops/ir/` materializa `IRDocument`, `IRBlock`, `ExtractionReceipt` e
  `BackendMapping` com hashes, locators, budgets e escrita atômica.
- Schemas canônicos e empacotados foram sincronizados; blocos estruturados não
  são reduzidos silenciosamente a texto.

## Verificação

- `tests/test_ir.py` passou, cobrindo invariantes, hash, parent/ordinal,
  atomicidade, idempotência e mapping.
- `scripts/sync_schemas.py --write` e `scripts/check_contracts.py --json`
  passaram para os 79 contratos registrados.
- Release gate core: 22/22 etapas passaram; a suíte principal teve 459
  passados e 6 skips explícitos; a integração RAGFlow real não faz parte desse
  perfil.

## Pendente

O aceite dependente de observações reais de parsing/locator do RAGFlow será
reaberto após o spike TK-001, sem promover essa ausência a sucesso.
