# TK-020 evidence — jornada e release candidate 2.0

Status: `in_progress`; jornada provider-free e gates core verdes; perfil
RAGFlow/handoff final ainda pendentes.

## Entregue

- `scripts/run_farol_v2_fixture.py` cobre projeto → fontes → IR → taxonomia →
  síntese → router → evidence → composição → rollback com saída redigida.
- `scripts/run_release_gates.py --profile core` foi endurecido para preservar
  interpretador isolado, auditar seams e produzir artefatos por etapa.

## Verificação

- Release core com commit capturado `81d5dcb2e189d00406cdd9b9e671d94e3f23cd58`
  e worktree atual: 22/22 etapas passaram, com `459 passed, 6 skipped` na
  suíte principal; wheel-core, clone limpo, crash-matrix (`17 passed`) e
  revocation (`25 passed`) passaram.
- Documentação (116 Markdown), schemas (79), contratos e Ruff passaram.
- O runner agora aceita `--profile ragflow` como perfil separado do `full`
  legado; o plano tem 23 etapas e a etapa final é fail-closed.
- A execução do perfil RAGFlow completou as 22 etapas core e falhou somente na
  etapa 23 com `status=blocked` e os quatro inputs externos ausentes; nenhum
  skip foi usado para transformar essa ausência em sucesso.

## Pendente

Uma execução direta do preflight RAGFlow retornou `blocked` por ausência de
`DOCOPS_RAGFLOW_ENDPOINT`, `DOCOPS_RAGFLOW_TOKEN`,
`DOCOPS_RAGFLOW_IMAGE_DIGEST` e `DOCOPS_RAGFLOW_SDK_VERSION`; o perfil
completo permanece impedido até esses recursos serem provisionados. Sem isso,
este é um release candidate provider-free, não um cutover final nem publicação
externa.
