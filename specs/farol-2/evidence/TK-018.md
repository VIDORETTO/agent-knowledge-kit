# TK-018 evidence — paridade e cutover

Status: `in_progress`; gate e fixture provider-free existem, mas o dual-run
RAGFlow real ainda está `not_run`.

## Entregue

- `evaluate_cutover()` exige Recall@5, MRR@5, coverage de citation/lineage,
  lifecycle, recovery, rollback e `ragflow_status=passed`; ausência externa
  retorna `not_run`, preservando o legado.
- Golden sintético e fixture cobrem jornada, citações e rollback sem corpus
  privado ou modelo.

## Verificação

- `tests/test_farol_v2_gates.py` e `tests/test_farol_v2_fixture.py` passaram.
- Release gate core: 22/22 etapas passaram; a suíte principal teve 459
  passados e 6 skips explícitos; wheel, clone limpo, crash-matrix e revocation
  também passaram.

## Pendente

Faltam dual-run com RAGFlow v0.27.2, métricas de golden revisado e receipt de
cleanup. O status não pode ser promovido a `cutover_approved` por inferência.
